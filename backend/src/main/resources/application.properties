# ??? existing (keep as is) ??????????????????????????
spring.datasource.url=jdbc:mysql://localhost:3307/wealth_advisor
spring.datasource.username=root
spring.datasource.password=Nishantp@27

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

server.port=8081

# ??? add these below ????????????????????????????????

# MySQL connection fix
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.format_sql=true

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# JWT
app.jwt.secret=wealthadvisor_super_secret_key_minimum_32_characters_long
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000
app.oauth2.redirect-uri=http://localhost:5173/oauth/success
app.google-oauth.client-id=${GOOGLE_CLIENT_ID:}
app.google-oauth.client-secret=${GOOGLE_CLIENT_SECRET:}
app.rate-limit.requests-per-minute=10

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
app.ml.base-url=http://localhost:5000
app.ml.timeout-ms=3000
app.market-data.market-csv-path=../recommendation-ml-service/data/market_data.csv
app.market-data.nav-csv-path=../recommendation-ml-service/data/nav_history.csv

# Mail (fill in later when you reach that feature)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=youremail@gmail.com
spring.mail.password=your_app_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
